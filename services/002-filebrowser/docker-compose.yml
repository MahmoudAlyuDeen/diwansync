services:
  filebrowser:
    user: 0:0
    volumes:
      - ../../storage/media/001-filebrowser:/media:rw
      - ../../storage:/storage:ro
      - ./config.yaml:/home/filebrowser/config.yaml
    ports:
      - '8002:80'
    image: gtstef/filebrowser
    restart: unless-stopped
