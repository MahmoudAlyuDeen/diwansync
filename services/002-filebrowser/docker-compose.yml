services:
  filebrowser:
    user: 0:0
    volumes:
      - /mnt:/mnt:rw
      - ../../storage/volumes/002-filebrowser/config.yaml:/home/filebrowser/config.yaml
    ports:
      - '8000:80'
    image: gtstef/filebrowser
    restart: unless-stopped
