# 005-authentik
POSTGRES_USER=authentik-005
POSTGRES_DB=authentik-005
POSTGRES_HOST=postgresql-005-authentik
REDIS_HOST=redis-005-authentik

# Run scripts/generate_secrets.sh to auto-generate secrets, or manually:
#   echo "POSTGRES_PASSWORD=$(openssl rand -hex 24)" >> storage.template/env/005-authentik.env
#   echo "AUTHENTIK_SECRET_KEY=$(openssl rand -hex 24)" >> storage.template/env/005-authentik.env
